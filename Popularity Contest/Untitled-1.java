// for (int i = 0; i < friends; i++) {
  // lista.add(scan.nextInt());
  // }
  // System.out.println(lista);

  // int popularityFactor = 0;
  // for (Integer factor : lista) {
  // for (Integer factor2 : lista) {
  // count++;
  // if (factor.equals(factor2)) {
  // lista.set(count, 0);
  // popularityFactor++;
  // if (factor2 == 0 || successFactor >= friends)
  // break;
  // }
  // }
  // if (successFactor > friends)
  // break;
  // marketabilityCoefficient = popularityFactor - successFactor;
  // popularityFactor = 0;
  // successFactor++;
  // count = -1;
  // System.out.println(marketabilityCoefficient);